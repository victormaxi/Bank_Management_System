@model _Core.ViewModels.AddImageVM

@{


}
<div class="col-md-10 mx-auto py-4">
    <div class="card-body">
        <form enctype="multipart/form-data" asp-action="ProfileImage" method="post">
            <div class="row">


                <div class="col-md-6">
                    <div class="form-group">
                        <label asp-for="Photo" class="control-label"></label>
                        <div>
                            <input class="form-control" name="Photo" type="file" />
                        </div>
                        <span asp-validation-for="Photo" class="text-danger"></span>


                    </div>
                </div>

            </div>

            <div class="form-group">
                <input type="submit" value="submit" class="btn btn-sm btn-primary rounded-0 text-uppercase" />
            </div>
        </form>
    </div>
</div>




@section scripts {
    <script type="text/javascript">
    //Add the following code if you want the name of the file appear on select
    $(".custom-file-input").on("change", function () {
        var fileName = $(this).val().split("\\").pop;
        $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
    });3

    $(document).ready(function () {
        $('input[type="Image"]').change(function (e) {
            var images = e.target.images[0].images;

        });
    });

    </script>
}